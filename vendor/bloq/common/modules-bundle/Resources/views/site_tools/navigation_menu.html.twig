<nav class="navegacion">
    <a href="{{ absolute_url("front_home") }}">Portada</a>
    {% if menu | length > 0 %}
    {% for section in menu %}
    <a href="{{ app.request.getSchemeAndHttpHost() ~ app.request.baseUrl ~ section.url }}">{{ section.name }}</a>
    {% endfor %}
    {% endif %}
</nav>
